## How to Run
1. Compile the code: `g++ main.cpp -o scheduler`
2. Ensure `processes.txt` is in the same directory.
3. Run the executable.

## Data Format
The `processes.txt` file uses the following format:
[Process ID] [Arrival Time] [Burst Time]
